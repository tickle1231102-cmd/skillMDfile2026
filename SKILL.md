---
name: ai-service-security-launch-check
description: Audit a web-based AI service before launch for security and operational safety. Use when asked to perform a pre-launch security check, review an AI SaaS or vibe-coded web app, verify Supabase/RLS/auth/API safety, inspect a deployment, or create a release security checklist.
---

# AI 서비스 출시 전 보안 점검

웹 기반 AI 서비스의 코드, 설정, 배포 환경을 출시 직전에 반복 점검한다. 실제 코드·설정·배포 정보를 확인한 뒤 결과를 작성한다. 확인하지 못한 항목은 통과로 처리하지 말고 `미확인`으로 표시한다.

## 작업 원칙

- 공개 저장소, 브라우저 번들, 클라이언트 코드에 비밀값을 노출하지 않는다.
- 프론트엔드를 신뢰 경계로 취급하지 않는다. 가격, 권한, 소유권, 사용량, 결제 상태는 서버에서 재검증한다.
- 취약점만 나열하지 말고 영향, 근거, 수정 위치, 출시 차단 여부를 함께 제시한다.
- 운영 환경과 스테이징 환경을 구분하고, 실제 운영 URL·환경변수·정책을 우선 확인한다.
- 민감한 키·토큰·개인정보는 결과에 그대로 출력하지 않는다. 키 이름과 노출 위치만 기록한다.
- 치명적 또는 높은 위험 항목이 남아 있으면 출시 승인을 내리지 않는다.

## 점검 절차

1. 서비스의 기술 스택, 인증 방식, 데이터 저장소, 결제, AI 모델/프록시, 배포 제공자, 외부 API를 파악한다.
2. 저장소에서 환경변수 사용, 인증 미들웨어, API 라우트, DB 정책/마이그레이션, 결제 웹훅, 파일 업로드, 프론트 요청 코드를 점검한다.
3. 배포 환경에서 도메인, HTTPS, CORS, 보안 헤더, 로그/오류 추적, 요금 한도와 알림을 점검한다.
4. 아래 체크리스트를 각 항목별로 `통과`, `실패`, `미확인`, `해당 없음`으로 판정한다.
5. 실패 항목은 심각도 순으로 정리하고, 가장 작은 안전한 수정안을 제시한다.
6. 최종적으로 출시 가능 여부를 `차단`, `조건부 가능`, `가능` 중 하나로 판정한다.

## 필수 체크리스트

### 비밀값과 설정

- [ ] API 키, 비밀번호, 토큰, 서비스 역할 키를 코드에 직접 쓰지 않고 환경변수로 관리하는가?
- [ ] `.env*`가 Git 추적 대상이 아니며, 저장소 이력과 프론트 번들에 비밀값이 없는가?
- [ ] 서버 전용 키(예: Supabase service role, 결제 비밀키, LLM API 키)를 클라이언트에 전달하지 않는가?
- [ ] 키가 노출된 적이 있다면 폐기·재발급하고 배포 환경도 교체했는가?
- [ ] 개발·스테이징·운영 환경의 키와 데이터가 분리되어 있는가?

### 인증과 인가

- [ ] 로그인이 필요한 데이터와 작업마다 서버에서 사용자 세션을 검증하는가?
- [ ] 사용자 ID, 조직 ID, 문서 ID를 클라이언트 입력 그대로 신뢰하지 않고 소유권·멤버십을 확인하는가? (IDOR 방지)
- [ ] 가격, 수량, 할인, 크레딧, 사용량, 역할, 결제 상태는 백엔드에서 계산·검증하는가?
- [ ] 관리자·운영자 기능은 별도 권한 검사를 하며, URL만 알아도 접근할 수 없지 않은가?
- [ ] 비밀번호 재설정, 이메일 인증, OAuth redirect URL, 세션 만료/로그아웃이 안전하게 구성되어 있는가?
- [ ] 쿠키 기반 인증이면 Secure, HttpOnly, SameSite를 적용하고 CSRF 보호가 필요한 요청을 방어하는가?

### 데이터베이스와 Supabase

- [ ] Supabase를 사용한다면 노출 가능한 테이블마다 RLS를 활성화했는가?
- [ ] RLS 정책이 `auth.uid()` 등으로 행 소유권/조직 멤버십을 제한하며, 읽기·생성·수정·삭제를 각각 검증하는가?
- [ ] anon 키만 클라이언트에 사용하고 service role 키는 서버에서만 사용하는가?
- [ ] Storage 버킷에도 업로드·다운로드·삭제 정책이 있고, 공개 버킷이 의도적인가?
- [ ] DB 함수·RPC·뷰가 권한을 우회하거나 과도한 권한으로 실행되지 않는가?
- [ ] 운영 DB 백업과 복구 절차, 보존 기간, 접근 권한을 확인했는가?

### API와 입력값

- [ ] 모든 서버 API가 인증, 인가, 입력 타입·길이·범위 검증을 수행하는가?
- [ ] API 호출별 rate limit, 사용자별 사용량 한도, 동시성 제한 또는 abuse 방어가 있는가?
- [ ] 검색·DB 쿼리가 파라미터 바인딩을 사용하고, 사용자 입력을 명령·쿼리·HTML에 직접 결합하지 않는가?
- [ ] 사용자 제공 HTML/Markdown/URL을 안전하게 처리해 XSS, open redirect, SSRF를 막는가?
- [ ] 파일 업로드가 파일 형식·크기·저장 위치를 검증하고, 업로드 파일을 실행하거나 공개 경로에 그대로 노출하지 않는가?
- [ ] 오류 응답이 스택 트레이스, 토큰, 내부 URL, 개인정보를 클라이언트에 노출하지 않는가?

### AI 기능

- [ ] LLM API 호출이 서버를 통해 이뤄져 공급자 API 키를 숨기는가?
- [ ] 프롬프트·업로드 파일·대화 기록에 포함된 개인정보와 고객 데이터를 최소화하고, 저장·공유·보존 방침을 알리는가?
- [ ] AI 도구 호출, 코드 실행, 브라우징, 외부 URL 접근에 허용 목록과 권한 검사를 적용하는가?
- [ ] 프롬프트 인젝션으로 시스템 지시·비밀·다른 사용자 데이터가 유출되지 않도록 신뢰하지 않는 입력을 분리하는가?
- [ ] 긴 입력/출력, 반복 요청, 악성 프롬프트가 토큰 비용이나 외부 API 비용을 폭증시키지 않도록 제한하는가?
- [ ] 고위험 결과(법률·의료·금융 조언, 자동 실행)는 사용자 검토나 안전장치를 거치게 하는가?

### 결제와 외부 서비스

- [ ] 결제 금액과 상품 권한을 결제 제공자의 서버 이벤트/검증 결과로 확정하는가?
- [ ] 결제 웹훅 서명을 검증하고, 재전송·중복 이벤트를 안전하게 처리하는가?
- [ ] 외부 OAuth/API 권한 범위는 필요한 최소 수준이며, 토큰 저장·회전·폐기가 가능한가?
- [ ] Webhook·외부 콜백 endpoint는 서명 검증 또는 비밀 토큰으로 보호하는가?

### 배포와 운영

- [ ] 운영 환경에서 HTTPS를 강제하고, HSTS·CSP·X-Content-Type-Options·frame-ancestors 등 적절한 보안 헤더를 설정했는가?
- [ ] CORS가 실제 허용 도메인으로 제한되고, credentials와 와일드카드 조합을 사용하지 않는가?
- [ ] 의존성 잠금 파일이 있고, 알려진 취약점과 오래된 핵심 패키지를 점검했는가?
- [ ] 에러 추적과 감사 로그에 비밀값·전체 요청 본문·개인정보를 기록하지 않는가?
- [ ] 이상 로그인, 권한 변경, 결제 실패, API 오류, 사용량 급증을 탐지할 수 있는가?
- [ ] 사용하는 무료 티어의 한도, 초과 시 동작, 결제 전환 조건, 요금 알림을 확인했는가?
- [ ] AI 모델, DB, 호스팅, 파일 저장소 각각에 비용 상한 또는 예산 알림을 설정했는가?
- [ ] 장애 시 비밀키 회전, 사용자 세션 무효화, 서비스 중지, 복구를 위한 담당자와 절차가 있는가?

## 결과 형식

다음 구조로 간결하게 보고한다.

### 출시 판정

`차단` | `조건부 가능` | `가능`

한두 문장으로 근거를 요약한다.

### 위험 항목

| 심각도 | 상태 | 항목 | 근거/위치 | 영향 | 최소 수정 |
| --- | --- | --- | --- | --- | --- |
| Critical/High/Medium/Low | 실패/미확인 | 항목 | 파일·설정·관찰 결과 | 가능한 피해 | 구체적 조치 |

### 통과한 보호 장치

확인된 보호 장치만 짧게 나열한다.

### 출시 전 남은 작업

차단 항목부터 체크박스로 정리한다. 각 항목에는 검증 방법을 덧붙인다.

## 심각도 기준

- **Critical**: 비밀키 노출, 인증 우회, 다른 사용자 데이터 전체 접근, 결제 권한 위조, 원격 코드 실행 가능성. 출시 차단.
- **High**: IDOR, RLS 부재/우회, 서버 검증 부재, 웹훅 미검증, 저장형 XSS, 심각한 API 남용 가능성. 출시 차단.
- **Medium**: 과도한 CORS, rate limit 부재, 로그 개인정보 노출, 부족한 보안 헤더, 비용 상한 부재. 수정 후 조건부 출시 가능.
- **Low**: 방어 심화, 문서화, 모니터링 보완. 출시 후 즉시 작업으로 관리 가능.
